use imgui::*;
use imgui_winit_support::{HiDpiMode, WinitPlatform};
use winit::{event_loop::EventLoop, window::WindowBuilder};

#[test]
fn test_imgui_window(){
    let event_loop
}
fn test_imgui_plots(){
}

#[test]
fn it_works() {
    assert!(my_crate::some_fn());
}

#[test]
fn it_works() {
    use my_crate::*;
    assert(some_fn());
}
